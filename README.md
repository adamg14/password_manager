# Passowrd Manager

## Zero-Knowledge, AES Encrypted, SHA-512 Hash Storage

## Testing
Run this command in the terminal to run the built in unit tests. 
```shell
chmod +x testing_script.sh
./testing_script.sh
```